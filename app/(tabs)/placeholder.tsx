import React from 'react';
import { View } from 'react-native';

// This is a placeholder screen for the center FAB tab
// It won't actually be shown as we intercept the tab press
export default function PlaceholderScreen() {
  return <View />;
}

